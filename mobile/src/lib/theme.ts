/// The app's palette.
///
/// The values and their roles live in the mirrored brand module, so the phone
/// and the website cannot disagree about what "accent" means. This file exists
/// to re-export them under the names the app already uses.
export { colors, TRIP_COLORS, RADIUS, PAINT, type Palette } from "@/lib/brand";
