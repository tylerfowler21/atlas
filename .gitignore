/node_modules
/.next/
/out/
/build
.DS_Store
*.pem
npm-debug.log*
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts
/src/generated
prisma/*.db
prisma/*.db-journal

# local data dumps
roava-export.json

# local dev Postgres (PGlite)
.pgdata/
# A dev database that has been set aside after corrupting. It is a Postgres
# data directory, so it holds real OAuth tokens from local sign-ins — GitHub's
# push protection is what caught one of these being committed.
.pgdata.*/

# iCloud Drive conflict copies
*[0-9].toml
*[0-9].ts
*[0-9].tsx
.vercel

# Brand source art. Large, and the generated assets in public/ are what ship.
brand/*.pdf

# A stray `create-expo-app roava` scaffold. The real app is in mobile/.
/roava/
